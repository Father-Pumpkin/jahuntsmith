---
import Base from '../../layouts/Base.astro';
import { getPublishedPosts } from '../../lib/content';

const posts = await getPublishedPosts();
const fmt = (d: string | null) =>
  d ? new Date(d).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : '';
---

<Base title="Blog — jahuntsmith" description="Writing by J. A. Huntsmith.">
  <div class="wrap">
    <div class="page-title"><h1>Blog</h1></div>
    {posts.length === 0 && <p style="color:var(--text-grey)">No posts yet. Check back soon.</p>}
    <ul class="post-list">
      {posts.map((p) => (
        <li>
          <span class="date">{fmt(p.published_at)}</span>
          <h3><a href={`/blog/${p.slug}`}>{p.title}</a></h3>
          {p.excerpt && <p class="excerpt">{p.excerpt}</p>}
        </li>
      ))}
    </ul>
  </div>
</Base>
